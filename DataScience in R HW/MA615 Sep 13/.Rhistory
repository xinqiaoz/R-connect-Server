library(knitr)                ## loading the entire knitr package
library(ggplot2)              ## the only tidyverse library you need is ggplot2
library(esquisse)             ## use esquisse to setup the basic plots
library(kableExtra)
library(magrittr)
opts_chunk$set(echo = TRUE)
data(mpg)     ## load the data
mpg$cyl <- as.factor(mpg$cyl)   ## convert discrete variables to factors so that they plot
mpg$drv <- as.factor(mpg$drv)   ## as different colors, not gradations of one color
mpg$cty <- as.double(mpg$cty)
class <- unique(mpg$class)      ## define a vector for car classes
cty_mean <- rep(0, length(class))  ## define variables to hold mean mileage
hwy_mean <- rep(0,length(class))   ## for each class of car
for(i in 1:length(class)){                          ## calculate the mean city and
cty_mean[i] = mean(mpg$cty[mpg$class==class[i]])  ## highway mileage for each
hwy_mean[i] = mean(mpg$hwy[mpg$class==class[i]])  ## class of car
}
cty_mean <- round(cty_mean, 2)                      ## round the results to 2 decimal pla
hwy_mean <- round(hwy_mean, 2)
tbl_1 <- cbind(class, cty_mean,hwy_mean)      ## form an array for the table
kable(tbl_1, format = "latex", booktabs=TRUE, digits = 2,     ## call kable to make the table
col.names = c("Class", "City", "Highway"),
caption = "Mean City and Highway MPG by Car Class" )
library(knitr)                ## loading the entire knitr package
library(ggplot2)              ## the only tidyverse library you need is ggplot2
library(esquisse)             ## use esquisse to setup the basic plots
library(kableExtra)
library(magrittr)
opts_chunk$set(echo = TRUE)
data(mpg)     ## load the data
mpg$cyl <- as.factor(mpg$cyl)   ## convert discrete variables to factors so that they plot
mpg$drv <- as.factor(mpg$drv)   ## as different colors, not gradations of one color
mpg$cty <- as.double(mpg$cty)
class <- unique(mpg$class)      ## define a vector for car classes
cty_mean <- rep(0, length(class))  ## define variables to hold mean mileage
hwy_mean <- rep(0,length(class))   ## for each class of car
for(i in 1:length(class)){                          ## calculate the mean city and
cty_mean[i] = mean(mpg$cty[mpg$class==class[i]])  ## highway mileage for each
hwy_mean[i] = mean(mpg$hwy[mpg$class==class[i]])  ## class of car
}
cty_mean <- round(cty_mean, 2)                      ## round the results to 2 decimal pla
hwy_mean <- round(hwy_mean, 2)
tbl_1 <- cbind(class, cty_mean,hwy_mean)      ## form an array for the table
kable(tbl_1, format = "latex", booktabs=TRUE, digits = 2,     ## call kable to make the table
col.names = c("Class", "City", "Highway"),
caption = "Mean City and Highway MPG by Car Class" )
library(knitr)                ## loading the entire knitr package
library(ggplot2)              ## the only tidyverse library you need is ggplot2
library(esquisse)             ## use esquisse to setup the basic plots
library(kableExtra)
library(magrittr)
opts_chunk$set(echo = TRUE)
data(mpg)
#mpg %<>% group_by()
force(mpg)
library(tidyverse)
library(esquisse)             ## use esquisse to setup the basic plots
library(kableExtra)
library(magrittr)
opts_chunk$set(echo = TRUE)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
kable(mpg_1)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
colnames(mpg_1)=c('Class','City MPG','Highway MPG')
kable(mpg_1)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
colnames(mpg_1)=c('Class','City MPG','Highway MPG')
kable(mpg_1,align = "c")
library(tidyverse)
library(esquisse)             ## use esquisse to setup the basic plots
library(kableExtra)
library(magrittr)
opts_chunk$set(echo = TRUE)
library(tidyverse)
library(knitr)
library(esquisse)
library(kableExtra)
library(magrittr)
opts_chunk$set(echo = TRUE)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
kable(mpg_1,format = 'latex',align =
col.names = c('Class','City MPG','Highway MPG'),
library(tidyverse)
library(knitr)
library(esquisse)
library(kableExtra)
library(magrittr)
library(tinytex)
opts_chunk$set(echo = TRUE)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = order(mpg_1,City,decreasing = TRUE)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = order(mpg_1$City,decreasing = TRUE)
kable(mpg_1,format = 'latex',align = "c",
col.names = c('Class','City MPG','Highway MPG'),
booktabs = TRUE,
digits = 2,
caption = "Average City and Highway MPG by Car Class")
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = order(mpg_1$City,decreasing = TRUE)
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = order(mpg_1$City,decreasing = TRUE)
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
mpg_1
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = mpg[order(mpg_1$City,decreasing = TRUE),]
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
mpg_1
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = mpg_1[order(mpg_1$City,decreasing = TRUE),]
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
mpg_1
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = mpg_1[order(mpg_1$City,decreasing = TRUE),]
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
kable(mpg_1)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = mpg_1[order(mpg_1$City,decreasing = TRUE),]
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
kable(mpg_1)
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = mpg_1[order(mpg_1$City,decreasing = TRUE),]
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
mpg_1
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = mpg_1[order(mpg_1$City,decreasing = TRUE),]
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
kable(mpg_1,format = 'latex')
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = mpg_1[order(mpg_1$City,decreasing = TRUE),]
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
kable(mpg_1,digits = 2,col.names = c('Class','City MPG','Highway MPG'), booktabs = TRUE)
View(mpg_1)
library(knitr)                ## loading the entire knitr package
library(ggplot2)              ## the only tidyverse library you need is ggplot2
library(esquisse)             ## use esquisse to setup the basic plots
library(kableExtra)
library(magrittr)
opts_chunk$set(echo = TRUE)
data(mpg)     ## load the data
mpg$cyl <- as.factor(mpg$cyl)   ## convert discrete variables to factors so that they plot
mpg$drv <- as.factor(mpg$drv)   ## as different colors, not gradations of one color
mpg$cty <- as.double(mpg$cty)
class <- unique(mpg$class)      ## define a vector for car classes
cty_mean <- rep(0, length(class))  ## define variables to hold mean mileage
hwy_mean <- rep(0,length(class))   ## for each class of car
for(i in 1:length(class)){                          ## calculate the mean city and
cty_mean[i] = mean(mpg$cty[mpg$class==class[i]])  ## highway mileage for each
hwy_mean[i] = mean(mpg$hwy[mpg$class==class[i]])  ## class of car
}
cty_mean <- round(cty_mean, 2)                      ## round the results to 2 decimal pla
hwy_mean <- round(hwy_mean, 2)
tbl_1 <- cbind(class, cty_mean,hwy_mean)      ## form an array for the table
kable(tbl_1, booktabs=TRUE, digits = 2,     ## call kable to make the table
col.names = c("Class", "City", "Highway"),
caption = "Mean City and Highway MPG by Car Class" )
data(mpg)
mpg_1=select(mpg,cty,hwy,class)
mpg_1 %<>% group_by(class)%>%summarise(City=mean(cty),Highway=mean(hwy))%>%arrange(class,City,Highway)
mpg_1 = mpg_1[order(mpg_1$City,decreasing = TRUE),]
# kable(mpg_1,format = 'latex',align = "c",
#       col.names = c('Class','City MPG','Highway MPG'),
#       booktabs = TRUE,
#       digits = 2,
#       caption = "Average City and Highway MPG by Car Class")
kable(mpg_1,digits = 2,col.names = c('Class','City MPG','Highway MPG'), booktabs = TRUE,caption = "Average City and Highway MPG by Car Class")
